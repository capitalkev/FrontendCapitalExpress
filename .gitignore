# # Creado para un proyecto de React (Vite) + Firebase

# ## Dependencias
# La carpeta node_modules contiene todas las librerías de npm.
# Se puede regenerar en cualquier momento con `npm install`.
/node_modules

# ## Archivos de Build
# La carpeta 'dist' es generada por el comando `npm run build`.
# No se debe subir el código compilado, solo el código fuente.
/dist
/build
.firebase
# ## Archivos de Logs
# Logs generados por npm, yarn o firebase.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
.firebaserc
*.log
firebase.json
# ## Variables de Entorno
# Archivos .env contienen secretos y claves de API.
# ¡Nunca deben ser subidos a un repositorio público!
.env
.env.local
.env.*.local

# ## Archivos de Sistema Operativo
# Archivos de metadatos de macOS y Windows.
.DS_Store
Thumbs.db

# ## Directorios de Editores de Código e IDEs
# Archivos de configuración local para editores como VS Code o WebStorm.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# ## Archivos de Test
# Reportes de cobertura de pruebas.
/coverage

.gitignore